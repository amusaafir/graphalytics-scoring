/**
 * Represents a data cell in the AlgorithmTable as it holds the value and score.
 */
class AlgorithmTableCell {
    constructor(value, score) {
        this.value = value;
        this.score = score;
    }
}

module.exports = AlgorithmTableCell;